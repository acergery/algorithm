// Catch That Cow (숨바꼭질)
#include <bits/stdc++.h>
using namespace std;

const int MX = 100005;
queue<int> q;
bool vis[MX];
int board[MX];


int main(void) {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, k;
    cin >> n >> k;

    vis[n] = true;
    q.push(n);
    while (!q.empty()) {
        int
    }

    return 0;
}